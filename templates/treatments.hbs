{{> header}}
	<div class="treatments">
		<section class="anxiety">
			<div class="wrapper">
				<div class="menu-wrapper">
					<h2 class="section-title">Anxiety</h2>
					<ul class="treatment-menu tabs-menu">
						{{#each anxiety}}
							<li class="{{#isEqual url-title "generalized-anxiety"}}active{{/isEqual}}"><a href="#{{url-title}}">{{title}}</a></li>
						{{/each}}
					</ul>
				</div><!-- .menu-wrapper -->
				<div class="tabs-content">
					{{#each anxiety}}
						<div class="treatment {{#isEqual url-title "generalized-anxiety"}}active{{/isEqual}}" id="{{url-title}}">
							{{{markdown}}}
						</div><!-- treatment -->
					{{/each}}
				</div><!-- .tabs-content -->
			</div><!-- .wrapper -->
		</section><!-- .anxiety -->
		<section class="depression">
			<div class="title-wrapper">
				<h2 class="section-title">Depression</h2>
			</div>
			<div class="wrapper">
				{{{depression.markdown}}}
			</div>
		</section><!-- .depression -->
	</div>
{{> footer}}